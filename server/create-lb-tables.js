// var server = require('./server');
// var ds = server.dataSources.mssqlqra;
// var lbTables = ['Usuario', 'Conquista'];
/*lbTables.forEach(function(entry) {
  , function(er) {
    ds.discoverModelProperties(entry, function (err, props) {
      console.log(props);
    });
  })
});*/
/*
ds.autoupdate().then(function (er) {
    if (!er){
        var fileName = "./sqlPadrao.sql";
        // ds.connector.execute( processSQLFile(fileName), null, function (err, obj) {
        //     if (err) console.error(err);
        // });
    }


});*/
//TODO: Alterei a criação do banco para '/server/boot/autoupdate.js'
